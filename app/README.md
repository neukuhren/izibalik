# app

Фронтенд IZIBALIK на Vite + React + TypeScript. См. корневой `README.md`.

## Скрипты

- `npm run dev` — dev-сервер
- `npm run build` — сборка
- `npm run preview` — предпросмотр
- `npm run lint` — oxlint
